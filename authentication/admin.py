"""
authentication/admin.py
=======================
No custom models to register here.
Django's User model is already registered in the admin by default.
"""
from django.contrib import admin
# No custom models in this app
